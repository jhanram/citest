pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Build started"
                sh 'date'
                sh 'ls -l'
                sh 'cat index.html'
                echo "Build completed"
            }
        }
    }
}
